react apps are made out of components.

component is a piece of UI with logic and appearance.

component is a javascript function, that returns a markup.

component starts with a capital letter.

export default specify main component in the file.

markup syntax is called JSX.

stricter than html: tags must be closed, cannot return multiple tags: either wrap with div or `<></>`.

`sudo apt install language-pack-en language-pack-en-base manpages`

in `/etc/default/locale`:

`LANG="en_GB.UTF-8"`

Check:

`locale -a | grep en_`



https://askubuntu.com/questions/380746/changing-ubuntu-servers-language-to-english
